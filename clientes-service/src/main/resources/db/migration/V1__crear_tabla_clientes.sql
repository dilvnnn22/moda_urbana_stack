CREATE TABLE clientes (
                          id BIGINT PRIMARY KEY AUTO_INCREMENT,
                          nombre VARCHAR(100),
                          apellido VARCHAR(100),
                          correo VARCHAR(100),
                          telefono VARCHAR(50)
);